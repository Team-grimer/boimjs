{
  "compilerOptions": {
    "strict": false,
    "incremental": true, // 컴파일시 이전 컴파일 파일이 있다면 변경된 부분만 체크해서 컴파일한다.
    "composite": true, // 이전 빌드 정보를 가지고 있어 다음 빌드를 더 빠르게 진행할 수 있다.
    "target": "es2016",
    "module": "commonjs",
    "allowJs": true,
    "checkJs": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
  },
  "include": [
    "./packages"
  ],
  "exclude": [
    "./node_modules",
    "./docs",
    "./examples"
  ]
}
